# Browser interface

The UI is a static page in `public/`. It posts the pasted URL to `/api/extract`, then renders the normalized conversation locally.

The visual system matches the ChatGPT extractor on purpose: same layout, same theme toggle, same copy / download / reader controls. Copy is branded for Grok share links only.
